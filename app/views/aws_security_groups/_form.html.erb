<%= form_for(@aws_security_group) do |f| %>
  <% if @aws_security_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aws_security_group.errors.count, "error") %> prohibited this aws_security_group from being saved:</h2>

      <ul>
      <% @aws_security_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.text_field :owner_id %>
  </div>
  <div class="field">
    <%= f.label :group_name %><br>
    <%= f.text_area :group_name %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.text_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
