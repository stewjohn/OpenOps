<%= form_for(@aws_az) do |f| %>
  <% if @aws_az.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aws_az.errors.count, "error") %> prohibited this aws_az from being saved:</h2>

      <ul>
      <% @aws_az.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :aws_region_id %><br>
    <%= f.number_field :aws_region_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
