<%= form_for(@checkpoint_frequency) do |f| %>
  <% if @checkpoint_frequency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@checkpoint_frequency.errors.count, "error") %> prohibited this checkpoint_frequency from being saved:</h2>

      <ul>
      <% @checkpoint_frequency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hours_apart %><br>
    <%= f.number_field :hours_apart %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
