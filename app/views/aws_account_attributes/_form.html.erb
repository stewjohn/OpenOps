<%= form_for(@aws_account_attribute) do |f| %>
  <% if @aws_account_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aws_account_attribute.errors.count, "error") %> prohibited this aws_account_attribute from being saved:</h2>

      <ul>
      <% @aws_account_attribute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :aws_account_id %><br>
    <%= f.number_field :aws_account_id %>
  </div>
  <div class="field">
    <%= f.label :attribute_name %><br>
    <%= f.text_field :attribute_name %>
  </div>
  <div class="field">
    <%= f.label :attribute_value %><br>
    <%= f.text_field :attribute_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
