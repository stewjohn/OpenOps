<%= form_for(@ebs_volume_tag) do |f| %>
  <% if @ebs_volume_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ebs_volume_tag.errors.count, "error") %> prohibited this ebs_volume_tag from being saved:</h2>

      <ul>
      <% @ebs_volume_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :volume_id %><br>
    <%= f.text_field :volume_id %>
  </div>
  <div class="field">
    <%= f.label :key %><br>
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
