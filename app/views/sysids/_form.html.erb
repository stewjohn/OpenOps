<%= form_for(@sysid, :html => {:class=> "form-horizontal" }) do |f| %>
  <% if @sysid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sysid.errors.count, "error") %> prohibited this sysid from being saved:</h2>

      <ul>
      <% @sysid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
    <%= f.label :name, :class => "col-sm-3 control-label"  %>
    <div class="col-sm-9">
        <%= f.text_field :name %>
    </div>
  </div>
    <div class="form-group">
    <%= f.label :owner, :class => "col-sm-3 control-label"  %>
      <div class="col-sm-9">
    <%= f.text_field :owner %>
  </div></div>
    <div class="form-group">
    <%= f.label :email_notification, :class => "col-sm-3 control-label"  %>
      <div class="col-sm-9">
    <%= f.text_field :email_notification %>
  </div></div>
    <div class="form-group">
      <label class="col-sm-3 control-label" > </label>
      <div class="col-sm-5">
        <div class="submit_link actions  "> <%= f.submit :class => 'btn btn-green' %> </div>
      </div>
    </div>
<% end %>
